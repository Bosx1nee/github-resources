var input = document.querySelector("");
var result = document.querySelector("");

//The new function 
function userText() {
    var inputText = document.getElementById("").value;
    document.getElementById("").innerHTML = inputText;
}

//Creates the highlight element and tells which color corresponds to how much words there are
text.oninput = function (){
    var box = document.createElement("");
    var sentence = text.value.match(/([^])\.!\?]+[\.!\?]+)|([^\.!\?]+$)/g)
    console.log(sentences);
    paragraph.innerHTML = "";
    
    for (var i = 0; i <sentence.length; i++);
    var sentence = sentences[i];
    var length = sentence.split("").length;
    highlight.textContent
}